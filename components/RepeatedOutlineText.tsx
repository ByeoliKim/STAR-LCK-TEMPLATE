"use client";

import clsx from "clsx";

type RepeatedOutlineTextProps = {
  text: string;
  repeat?: number;
  className?: string;
  align?: "left" | "center" | "right";
  fillColor: string;
  strokeColor?: string;
};
